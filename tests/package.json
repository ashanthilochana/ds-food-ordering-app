{
  "name": "food-ordering-app-tests",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "mocha ./**/*.test.js --timeout 10000",
    "test:selenium": "mocha ./selenium/**/*.test.js --timeout 10000",
    "test:ui": "mocha ./ui/**/*.test.js --timeout 10000"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "selenium-webdriver": "^4.15.0",
    "chromedriver": "^135.0.0"
  }
}
